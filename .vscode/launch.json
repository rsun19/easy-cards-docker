{
    "configurations": [
        {
            "name": "Docker Node.js Launch",
            "type": "docker",
            "request": "launch",
            "preLaunchTask": "docker-run: debug",
            "platform": "node",
            "node": {
                "package": "${workspaceFolder}/easy-cards/package.json",
                "localRoot": "${workspaceFolder}/easy-cards"
            }
        }
    ]
}